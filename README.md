![GitHub repo size](https://img.shields.io/github/repo-size/kymppi/webhook-listener)

# Webhook Listener

A Simple server that writes the received data to a JSON file, could be used as a webhook listener.

